import { SidebarHeaderClickDirective } from './sidebar-header-click.directive';

describe('SidebarHeaderClickDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarHeaderClickDirective();
    expect(directive).toBeTruthy();
  });
});
