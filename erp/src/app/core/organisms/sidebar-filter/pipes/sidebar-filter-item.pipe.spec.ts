import { SidebarFilterItemPipe } from './sidebar-filter-item.pipe';

describe('SidebarFilterItemPipe', () => {
  it('create an instance', () => {
    const pipe = new SidebarFilterItemPipe();
    expect(pipe).toBeTruthy();
  });
});
