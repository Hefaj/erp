import { GridTemplateAreasPipe } from './grid-template-areas.pipe';

describe('GridTemplateAreasPipe', () => {
  it('create an instance', () => {
    const pipe = new GridTemplateAreasPipe();
    expect(pipe).toBeTruthy();
  });
});
