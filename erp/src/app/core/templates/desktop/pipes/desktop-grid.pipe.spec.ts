import { DesktopGridPipe } from './desktop-grid.pipe';

describe('DesktopGridPipe', () => {
  it('create an instance', () => {
    const pipe = new DesktopGridPipe();
    expect(pipe).toBeTruthy();
  });
});
